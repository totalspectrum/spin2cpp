#include <propeller.h>
#include "test63.h"

int32_t test63::test(int32_t exponent)
{
  int32_t result = 0;
  if (exponent == 0) {
    (++exponent);
  } else if (1) {
    (--exponent);
  }
  return result;
}

