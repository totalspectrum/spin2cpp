

PUB lookupdowns(i)
  outa := lookup(i : 1,2,3)
  outa := lookup(i : ina,inb)
  outa := lookup(i : "abc")

  outa := lookdown(i : 1,2,3)
  outa := lookdown(i : ina,inb)
  outa := lookdown(i : "abc")

  outa := lookdownz(i: "abc",1,2,3,ina)
